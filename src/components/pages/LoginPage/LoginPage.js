import React from 'react';

const LoginPage = () => (
  <div>
    <h1>Login page</h1>

    <div>
      <input type="text"/>
      <input type="password"/>
      <button>Login</button>
    </div>
  </div>
);

export default LoginPage;
